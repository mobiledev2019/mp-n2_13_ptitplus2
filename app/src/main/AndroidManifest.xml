<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.n2_13_ptitplus2">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_api_key"/>

        <activity android:name=".TinTucHoiThaoActivity"></activity>
        <activity android:name=".TinTucNghienCuuActivity" />
        <activity android:name=".TinTucTapChiActivity" />
        <activity android:name=".TinTucChienLuocActivity" />
        <activity android:name=".NganhHocActivity" />
        <activity android:name=".TuyenBusActivity" />
        <activity android:name=".BanDoActivity" />
        <activity android:name=".TinTucActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>